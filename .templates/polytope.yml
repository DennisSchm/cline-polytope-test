modules:
  - id: create-component
    module: polytope/scaffold
    params:
      - id: template
        info: Name of the component to create
        type: str
      - id: path
        info: Path where the component should be created
        type: str
    args:
      path: pt.param path
      template: ".templates/{pt.param template}"
